output: dist/
releases:
  - name: dev
    jobs:
      - name: release-appimage
        package:
          platform: linux
          target: appimage
          build_args:
            enable-experiment: records
